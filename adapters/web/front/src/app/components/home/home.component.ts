import { Component } from '@angular/core';

// This component is no longer used — replaced by ShellComponent + page components.
@Component({
  selector: 'app-home',
  standalone: true,
  template: '',
})
export class HomeComponent {}
